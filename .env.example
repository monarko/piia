# This .env file was generated by buffalo, add here the env variables you need 
# buffalo to load into the ENV on application startup so your application works correctly.
# To add variables use KEY=VALUE format, you can later retrieve this in your application
# by using os.Getenv("KEY").
#
# Example:
# DATABASE_PASSWORD=XXXXXXXXX
# SESSION_SECRET=XXXXXXXXX
# SMTP_SERVER=XXXXXXXXX
# GO_ENV=production
SESSION_SECRET=
APP_HOST=http://127.0.0.1

DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=postgres

EMAIL_SENDER="no-reply@example.com"
EMAIL_NAME="PIIA (peer)"

MAILGUN_DOMAIN=""
MAILGUN_API_KEY=""

# API Key and Secret for Google Login
GOOGLE_KEY=""
GOOGLE_SECRET=""

# Storage bucket name which holds fundoscopy images and export bucket name to export analytics
GOOGLE_STORAGE_BUCKET_NAME=""
GOOGLE_STORAGE_EXPORT_BUCKET_NAME=""

# Credentials JSON file path to read the fundoscopy images. Only storage.object.read permission required
GOOGLE_APPLICATION_CREDENTIALS_PATH=""

# Credentials JSON file path to write exported CSV. Only storage.object.write permission required
GOOGLE_APPLICATION_CREDENTIALS_PATH_FOR_EXPORT=""

HOSPITALS="Pathum Thani District Hospital, Rajavithi Hospital, Nakhorn Ping Hospital"
HOSPITAL_NOT_REFERRAL_REASONS="Ungradeable image,Suspected cataract with non-referral VA,Patient referred to in-house ophthalmologist,Referral refused by participant"
HOSPITAL_NOT_REFERRAL_REASONS_THAI="ประเมินผลไม่ได้ Ungradable image,สงสัยต้อกระจกที่มีค่าสายตาดี Suspected cataract with non-referral VA,คนไข้ถูกส่งตัวไปเจอจักษุแพทย์ในโรงพยาบาล Patient referred to in-house opthalmologist,คนไข้ปฏิเสธที่จะส่งตัว Referral refused by participant"
